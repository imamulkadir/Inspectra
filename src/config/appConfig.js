export const APP_VERSION = "0.1.0";

export const DB_NAME = "inspectra-db";
export const DB_VERSION = 1;

export const STORES = Object.freeze({
  inspections: "inspections",
  evidence: "evidence",
  reports: "reports",
  settings: "settings",
});
